name: Compilar OrangeFox Pettyl (Official Script)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Limpiar espacio en disco
      uses: jlumbroso/free-disk-space@v1.3.1

    - name: Instalar Dependencias
      run: |
        sudo apt-get update
        sudo apt-get install -y bc build-essential zip curl libc6-dev libncurses5 libncurses5-dev libssl-dev x11proto-core-dev libx11-dev libgl1-mesa-dev g++-multilib libxml2-utils xsltproc unzip python2 rsync git

    - name: Configurar Repo Tool
      run: |
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        echo "$HOME/bin" >> $GITHUB_PATH

    - name: Sincronizar Fuentes (Método Oficial Sync)
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        # 1. Obtener las herramientas de sincronización de GitLab
        mkdir ~/OrangeFox_sync
        cd ~/OrangeFox_sync
        git clone https://gitlab.com/OrangeFox/sync.git
        
        # 2. Ejecutar el script (Usamos 12.1 porque es la rama estable soportada)
        cd sync
        ./orangefox_sync.sh --branch 12.1 --path $HOME/fox_12.1
        
    - name: Clonar Device Tree
      run: |
        cd $HOME/fox_12.1
        git clone https://github.com/elmendezz/android_device_motorola_pettyl -b master device/motorola/pettyl

    - name: Compilar Recovery
      run: |
        cd $HOME/fox_12.1
        export ALLOW_MISSING_DEPENDENCIES=true
        export LC_ALL="C"
        source build/envsetup.sh
        
        # En el entorno 12.1, el prefijo suele ser twrp_
        lunch twrp_pettyl-eng || lunch fox_pettyl-eng || lunch omni_pettyl-eng
        
        # Usamos mka en lugar de make, y compilamos adbd y recoveryimage
        mka adbd recoveryimage -j$(nproc --all)

    - name: Publicar Release
      uses: softprops/action-gh-release@v2
      if: success()
      with:
        tag_name: orangefox-pettyl-${{ github.run_number }}
        name: OrangeFox Recovery Pettyl
        files: |
          /home/runner/fox_12.1/out/target/product/pettyl/OrangeFox-*.zip
          /home/runner/fox_12.1/out/target/product/pettyl/recovery.img
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
